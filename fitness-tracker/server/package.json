{
  "name": "fitness-tracker-server",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "server.js",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.1",
    "pg": "^8.6.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../server/public && cp -a dist/. ../server/public/",
    "start": "cd server && node server.js"
  }
}
